[project]
name = "mdict"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jadren", email = "jadren@jadren.dev" }
]
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "mdict-utils>=1.3.14",
]

[project.scripts]
mdict = "mdict:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
